#pragma once

#include<string>
#include<sstream>
namespace MWCDTool {
    std::string doubleToString(const double& num);
    double keepTwoDecimal(const double& num);
    std::string intToString(const int& num);
    int doubleToInt(const double& num);
}

